services:
  hubspot-mcp:
    build: .
    restart: unless-stopped
    ports:
      - "8003:3000"
    environment:
      PORT: 3000
      TELEMETRY_ENABLED: "false"
    networks:
      - mcp-shared

networks:
  mcp-shared:
    external: true
